import React from 'react'
import Display from './App.js'

//Dynamic rendering && and || //|| not good for rendering dynamically, to oaccepting of values

ReactDOM.render(<App />, document.getElementById('root'))
    )